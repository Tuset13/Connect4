<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.connect4"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-sdk tools:overrideLibrary="android.support.fragment"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".OldGames.OldGamesActivity">

        </activity>
        <activity android:name=".OldGames.DetailActivity">

        </activity>
        <activity android:name=".HelpActivity">

        </activity>
        <activity android:name=".ResultsActivity">

        </activity>
        <activity android:name=".Game.GameActivity">

        </activity>
        <activity android:name=".Preferences.PreferencesActivity">

        </activity>
    </application>

</manifest>